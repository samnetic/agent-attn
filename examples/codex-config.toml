notify = ["bash", "-lc", "~/.local/bin/agent-attn --window 'codex-main' --app 'Codex' --event 'agent-turn-complete' --message 'Codex needs attention'"]

[tui]
notifications = ["approval-requested", "agent-turn-complete"]
notification_method = "bel"
